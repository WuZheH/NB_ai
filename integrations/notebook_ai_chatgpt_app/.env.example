# NOTEBOOK_AI backend. Plain HTTP is accepted only for a loopback host.
NOTEBOOK_AI_BACKEND_URL=http://127.0.0.1:8000

# Required for the first, explicitly unauthenticated Developer Mode build.
# Never leave this enabled on an internet-facing host.
NOTEBOOK_AI_ALLOW_UNAUTHENTICATED_MCP_DEV=1

# MCP listens on loopback only. An HTTPS tunnel may forward to this port.
NOTEBOOK_AI_MCP_PORT=8787

# Optional backend credential extension point. Do not commit a real value.
# NOTEBOOK_AI_BACKEND_BEARER_TOKEN=

# Optional dedicated widget origin for a future hosted/public build.
# NOTEBOOK_AI_WIDGET_DOMAIN=https://widgets.example.com
